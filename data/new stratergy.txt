
https://github.com/tradytics/surpriver

Here’s a clear breakdown of the GitHub repo you shared:

🔗 Repo: tradytics/surpriver
🧠 What it is
Surpriver is an algorithmic trading / ML project built by Tradytics that aims to:

Find stocks that are about to make big moves before they actually move

It does this using:

📊 Machine learning

⚠️ Anomaly detection

📈 Market data patterns (price, volume, volatility, etc.)

👉 In simple terms:
It tries to detect “unusual behavior” in stocks early, which could signal upcoming breakout / breakdown moves. 

⚙️ Core Idea (Very Important)
The system assumes:

If a stock behaves abnormally vs its historical pattern → something is brewing (news, accumulation, institutional activity)

So it:

Collects stock data

Creates features (returns, volatility, etc.)

Runs anomaly detection models

Flags “outliers” = potential big movers

🧩 Key Components (from repo structure & contributions)
Based on available info, main modules include:

1. data_loader.py
Loads historical market data

Prepares datasets for model

2. detection_engine.py
Core ML logic

Detects anomalies in stock behavior

3. Feature Engineering
Volatility filters

Return patterns

Possibly rolling statistics

4. Backtesting / Future testing
Helps validate if signals actually work

Important for avoiding false positives

👉 These were actively improved (bug fixes, filters, refactoring) by the author. 

🧠 Strategy Type (Important for you)
This is NOT:

❌ Traditional technical analysis

❌ Pure fundamental investing

This IS:

✅ Quant + ML anomaly strategy

✅ Early signal detection system

✅ Best suited for:

Swing trades

Positional trades

Momentum breakout hunting

🧪 How You Can Use It (Practical)
Since you already collect NSE data, this is very relevant to you 👇

Use Case Ideas:
Bottom Hunting (your interest)

Detect unusual volume spikes near lows

Combine with delivery %

Breakout Prediction

Identify stocks with abnormal volatility compression → expansion

Institutional Activity Proxy

Use anomaly score + delivery data

Ranking Engine (Advanced)

Add anomaly score as a factor in your scoring engine

⚠️ Limitations (Important)
It does NOT guarantee predictions

Works best when:

Combined with other signals

Used as a filter, not final decision

Also:

Market anomalies can be noise → false signals

🧠 How This Fits Your Current Work
You’re building:

Smart positional scoring engine (greedy/fearful logic)

👉 This repo gives you:

A quant signal layer (very valuable)

Suggested Integration:
Add:

Anomaly Score Weight = 15–25%
Combine with:

Delivery data (you already have)

Volume expansion

Price structure (higher lows, base)

🚀 If You Want Next Step
I can:

🔧 Break down the actual code (file by file)

🧠 Help you rebuild this in NSE context

⚙️ Integrate into your combined scoring engine

📊 Add anomaly detection using your 3-year dataset

Just tell me:
👉 “rebuild surpriver for NSE” or
👉 “integrate into my engine”


rebuild surpriver for NSE

Good — this is exactly the right direction for what you're building.

Let’s rebuild Surpriver for NSE, but not blindly. We’ll adapt it to your positional + smart accumulation strategy (greedy/fearful logic + delivery data edge).

🧠 CORE PHILOSOPHY (Your Version)
We don’t just detect anomalies.

We detect:

“Smart money behavior before price moves”

So your NSE version will combine:

📦 Price anomalies

📊 Volume anomalies

📬 Delivery anomalies (your edge)

🧠 Context (trend, bottom, expansion phase)

⚙️ SYSTEM ARCHITECTURE (NSE Surpriver v2)
1. Data Inputs (You already have)
Use:

Daily OHLC

Volume

Delivery %

Delivery quantity

(Optional: Index data like NIFTY)

🧮 STEP 1: FEATURE ENGINEERING
We convert raw data → signals.

🔹 A. Price Features
df['return_1d'] = df['close'].pct_change()
df['return_5d'] = df['close'].pct_change(5)

df['volatility_10'] = df['return_1d'].rolling(10).std()
df['volatility_30'] = df['return_1d'].rolling(30).std()
🔹 B. Volume Anomaly
df['vol_ma20'] = df['volume'].rolling(20).mean()
df['volume_spike'] = df['volume'] / df['vol_ma20']
🔹 C. Delivery Edge (VERY IMPORTANT)
df['deliv_ma20'] = df['delivery_qty'].rolling(20).mean()
df['delivery_spike'] = df['delivery_qty'] / df['deliv_ma20']

df['delivery_ratio'] = df['delivery_qty'] / df['volume']
👉 This is your institutional footprint proxy

🔹 D. Compression → Expansion Setup
df['range'] = df['high'] - df['low']
df['range_ma20'] = df['range'].rolling(20).mean()

df['compression'] = df['range'] / df['range_ma20']
Low compression → future breakout candidate

🤖 STEP 2: ANOMALY ENGINE
Now we detect “unusual behavior”

Option 1 (Recommended): Isolation Forest
from sklearn.ensemble import IsolationForest

features = [
    'return_1d',
    'return_5d',
    'volume_spike',
    'delivery_spike',
    'delivery_ratio',
    'compression'
]

model = IsolationForest(contamination=0.05)
df['anomaly_score'] = model.fit_predict(df[features])
👉 Output:

-1 → anomaly (important)

1 → normal

Option 2 (Better for scoring): Z-score method
def zscore(series):
    return (series - series.mean()) / series.std()

df['z_volume'] = zscore(df['volume_spike'])
df['z_delivery'] = zscore(df['delivery_spike'])
df['z_return'] = zscore(abs(df['return_1d']))
🧠 STEP 3: SMART SCORING ENGINE (KEY DIFFERENCE)
We don’t just detect anomaly — we classify it

🎯 A. Accumulation Signal (Greedy Mode)
accumulation_score = (
    0.4 * df['z_delivery'] +
    0.3 * df['z_volume'] +
    0.3 * (1 - abs(df['return_1d']))
)
👉 Meaning:

High delivery + high volume

But price NOT moving much → accumulation

🚀 B. Breakout Signal
breakout_score = (
    0.4 * df['z_volume'] +
    0.3 * df['z_return'] +
    0.3 * (1 - df['compression'])
)
🩸 C. Distribution Signal (Fear Mode)
distribution_score = (
    0.5 * df['z_volume'] +
    0.3 * abs(df['return_1d']) +
    -0.2 * df['z_delivery']
)
👉 High volume + low delivery = dumping

🧭 STEP 4: MARKET CONTEXT FILTER (CRITICAL)
Bottom Detection (your style)
df['near_52w_low'] = df['close'] / df['close'].rolling(252).min()
Use:

< 1.2 → near bottom zone

Trend Filter
df['ma50'] = df['close'].rolling(50).mean()
df['trend'] = df['close'] > df['ma50']
🏁 STEP 5: FINAL NSE SURPRIVER SIGNAL
df['final_score'] = (
    0.35 * accumulation_score +
    0.35 * breakout_score +
    0.15 * (df['near_52w_low'] < 1.2) +
    0.15 * df['trend']
)
🧪 OUTPUT: STOCK CLASSIFICATION
Score	Meaning
> 0.7	🚀 Strong Buy Candidate
0.5–0.7	👀 Watchlist
< 0.3	❌ Ignore
🔥 WHAT MAKES YOUR VERSION SUPERIOR
Compared to original Surpriver:

Feature	Original	Your NSE Version
ML anomaly	✅	✅
Delivery data	❌	✅ (BIG EDGE)
Context aware	❌	✅
Accumulation logic	❌	✅
Bottom hunting	❌	✅
🚀 NEXT LEVEL (If you want)
We can upgrade further:

1. Multi-day accumulation detection
Detect 3–10 day silent accumulation

2. Sector-relative anomaly
Compare stock vs sector

3. RL-based adaptive weights
Auto-adjust scoring weights (your earlier idea)

4. Entry/Exit signals
Convert this into tradable system

👉 Tell me next step
Choose one:

“build full python pipeline” → complete working code

“add delivery + NSE dataset integration”

“convert into trading signals (entry/exit)”

“connect to your existing scoring engine”

We can take this to a serious quant system level.


onvert into trading signals (entry/exit)

Perfect — now we turn your NSE Surpriver engine → actual tradable system.

This is where most systems fail.
We’ll make yours practical, disciplined, and aligned with positional trading.

🧠 CORE IDEA (VERY IMPORTANT)
We don’t trade anomalies directly.

We trade:

“Confirmed smart money moves after anomaly appears”

So:

Anomaly = early alert

Entry = confirmation

Exit = risk control + trend exhaustion

⚙️ SIGNAL PIPELINE (FINAL STRUCTURE)
Anomaly → Accumulation → Confirmation → Entry → Hold → Exit
🟢 ENTRY SYSTEM (BUY SIGNAL)
✅ Step 1: Pre-Filter (Quality Stocks Only)
df['valid_zone'] = (
    (df['near_52w_low'] < 1.3) &   # near bottom
    (df['delivery_ratio'] > 0.35)  # strong hands
)
✅ Step 2: Accumulation Trigger
df['acc_signal'] = (
    (df['z_delivery'] > 1.2) &
    (df['z_volume'] > 1.0) &
    (abs(df['return_1d']) < 0.02)
)
👉 Meaning:

High delivery + volume

Price not moving → absorption

✅ Step 3: Confirmation (CRITICAL)
df['breakout_confirm'] = (
    (df['close'] > df['high'].rolling(5).max().shift(1)) &
    (df['volume_spike'] > 1.5)
)
👉 This avoids:

Fake accumulation

Dead stocks

🚀 FINAL ENTRY SIGNAL
df['BUY'] = (
    df['valid_zone'] &
    df['acc_signal'].rolling(3).max() &  # accumulation happened recently
    df['breakout_confirm']
)
📍 ENTRY EXECUTION
When BUY == True:

👉 Entry options:

Conservative: Next day breakout high

Aggressive: Same day close

🔴 EXIT SYSTEM (VERY IMPORTANT)
We use 3 types of exits:

1️⃣ STOP LOSS (Protection)
df['stop_loss'] = df['low'].rolling(5).min()
👉 OR fixed:

8–12% positional SL

2️⃣ TREND EXIT (Smart Exit)
df['trend_break'] = df['close'] < df['ma20']
👉 Exit when:

Price loses short-term trend

3️⃣ DISTRIBUTION EXIT (Smart Money Leaving)
df['dist_signal'] = (
    (df['z_volume'] > 1.5) &
    (df['z_delivery'] < 0) &
    (df['return_1d'] < -0.02)
)
👉 Meaning:

High volume

Low delivery

Price falling → dumping

🛑 FINAL EXIT SIGNAL
df['SELL'] = (
    df['trend_break'] |
    df['dist_signal']
)
💰 POSITION MANAGEMENT (EDGE)
🎯 Position Sizing
Setup Strength	Capital Allocation
Strong (score > 0.7)	20–25%
Medium	10–15%
Weak	Skip
📈 Add-on Strategy (VERY POWERFUL)
Add more when:

df['add_position'] = (
    (df['close'] > df['high'].rolling(10).max()) &
    (df['volume_spike'] > 1.3)
)
👉 Pyramid winners, not losers

🧪 FULL TRADE EXAMPLE
🟢 Phase 1: Accumulation
Delivery ↑

Volume ↑

Price sideways

👉 No trade yet

🟡 Phase 2: Breakout
Price breaks 5-day high

Volume spike

👉 ✅ ENTRY

🔵 Phase 3: Trend
Higher highs

Hold position

👉 Add on breakouts

🔴 Phase 4: Exit
Trend breaks OR

Distribution appears

👉 ✅ EXIT

⚠️ COMMON MISTAKES (AVOID THIS)
❌ Buying during accumulation (too early)
❌ Ignoring delivery data
❌ Holding after distribution
❌ No stop loss

🧠 FINAL EDGE (YOUR SYSTEM)
What makes your system powerful:

✅ Detects hidden accumulation

✅ Waits for confirmation

✅ Uses delivery (retail can’t see this easily)

✅ Avoids fake breakouts

✅ Exits on smart money exit

🚀 NEXT LEVEL (HIGHLY RECOMMENDED)
We can now:

1. Backtest this on your 3-year NSE data
👉 See real accuracy

2. Convert into:
Live screener

Daily signal generator

3. Add:
Sector strength filter

Index trend filter (NIFTY 50 bias)

👉 Tell me next step
Say one:

“backtest this system”

“convert into screener (daily signals)”

“optimize win rate / reduce drawdown”

We’re now very close to a serious alpha system.


Detect 3–10 day silent accumulation

Good — this is where your system becomes elite level.

You’re trying to detect:

“Silent accumulation over multiple days before breakout”

Not 1-day spikes… but consistent, hidden buying.

🧠 WHAT IS “SILENT ACCUMULATION” (CLEAR DEFINITION)
A stock is under accumulation when:

📬 Delivery stays consistently high

📊 Volume is above normal (not crazy spikes)

📉 Price stays tight / sideways

🧱 Dips get bought quickly (absorption)

👉 Institutions accumulate without moving price too much.

⚙️ DETECTION LOGIC (3–10 DAY WINDOW)
We build this in layers 👇

🧮 STEP 1: BASE FEATURES (Refined)
df['vol_ratio'] = df['volume'] / df['volume'].rolling(20).mean()
df['deliv_ratio'] = df['delivery_qty'] / df['volume']

df['price_range'] = (df['high'] - df['low']) / df['close']
df['range_ma'] = df['price_range'].rolling(20).mean()

df['tight_range'] = df['price_range'] < df['range_ma']
🧠 STEP 2: DAILY ACCUMULATION SCORE
We score each day:

def norm(x):
    return (x - x.mean()) / x.std()

df['z_vol'] = norm(df['vol_ratio'])
df['z_deliv'] = norm(df['delivery_qty'])

df['daily_acc_score'] = (
    0.45 * df['z_deliv'] +          # strong delivery
    0.30 * df['z_vol'] +            # above avg volume
    0.25 * (1 - abs(df['return_1d']))  # low price movement
)
🔁 STEP 3: MULTI-DAY ACCUMULATION DETECTION
🎯 Rolling Window (3–10 days)
window = 7  # can vary 3–10

df['acc_days_count'] = (
    (df['daily_acc_score'] > 0.5).rolling(window).sum()
)

df['tight_days'] = df['tight_range'].rolling(window).sum()
✅ SILENT ACCUMULATION CONDITION
df['silent_accumulation'] = (
    (df['acc_days_count'] >= window * 0.6) &   # at least 60% days strong
    (df['tight_days'] >= window * 0.6) &       # price stayed tight
    (df['vol_ratio'].rolling(window).mean() > 1.1)  # sustained volume
)
🧱 STEP 4: ABSORPTION DETECTION (VERY POWERFUL)
This is your edge layer 👇

df['buying_wicks'] = (
    (df['close'] > df['open']) &
    ((df['low'] < df['low'].shift(1)))
)

df['absorption'] = df['buying_wicks'].rolling(window).sum() > (window * 0.4)
👉 Meaning:

Price dips but closes strong → buyers absorbing supply

🚀 FINAL SILENT ACCUMULATION SIGNAL
df['SMART_ACCUMULATION'] = (
    df['silent_accumulation'] &
    df['absorption']
)
📊 SIGNAL STRENGTH (RANKING)
df['acc_strength'] = (
    df['acc_days_count'] / window +
    df['tight_days'] / window +
    df['absorption'].astype(int)
) / 3
🎯 HOW TO USE THIS IN TRADING
❌ DON’T BUY HERE
This is the biggest mistake.

👉 During accumulation:

Price will frustrate you

Looks “dead”

✅ WHAT TO DO INSTEAD
1. Put in Watchlist
if SMART_ACCUMULATION:
    tag = "READY_ZONE"
2. Wait for Breakout (Entry trigger)
Reuse your earlier:

BREAKOUT = (
    close > 5_day_high &
    volume_spike > 1.5
)
3. Combine Both
FINAL_BUY = (
    SMART_ACCUMULATION.shift(1) &
    BREAKOUT
)
👉 This is high probability entry

🔥 VISUAL INTUITION
Silent accumulation looks like:

Day 1–7:
Price:   ────┐ ────┐ ────┐ (sideways)
Volume:  ↑ ↑ ↑ (steady)
Delivery: ↑↑ ↑↑ ↑↑ (consistently high)

Then:
🚀 Breakout
⚠️ FILTER OUT FAKE CASES
Avoid:

❌ Illiquid stocks
df['volume'].rolling(20).mean() > threshold
❌ News spikes
Remove days with extreme returns (>8%)

🧠 PRO UPGRADE (VERY POWERFUL)
Add time decay scoring:

Recent accumulation matters more:

weights = np.linspace(1, 2, window)

df['weighted_acc'] = (
    (df['daily_acc_score'] * weights).rolling(window).sum() / weights.sum()
)
🚀 FINAL EDGE YOU JUST BUILT
You can now detect:

📦 Institutional accumulation (multi-day)

🧱 Absorption zones

⏳ Pre-breakout bases

👉 This is exactly how big money operates


DETECTION LOGIC (5–30 DAY WINDOW)

Nice — moving to a 5–30 day window is a big upgrade.
This captures real institutional accumulation cycles, not just short-term noise.

We’ll build this as a multi-timescale detection engine (very powerful).

🧠 CORE IDEA (ADVANCED)

Instead of one fixed window (like 7 days), we:

Scan multiple windows (5, 10, 15, 20, 30)
Detect accumulation consistency across them
Score strength based on persistence

⚙️ STEP 1: BASE FEATURES (Same foundation, slightly refined)
df['vol_ratio'] = df['volume'] / df['volume'].rolling(20).mean()
df['deliv_ratio'] = df['delivery_qty'] / df['volume']

df['return_1d'] = df['close'].pct_change()

df['price_range'] = (df['high'] - df['low']) / df['close']
df['range_ma'] = df['price_range'].rolling(20).mean()
df['tight_range'] = df['price_range'] < df['range_ma']
🧮 STEP 2: DAILY ACCUMULATION SCORE (Improved)
def zscore(x):
    return (x - x.mean()) / x.std()

df['z_vol'] = zscore(df['vol_ratio'])
df['z_deliv'] = zscore(df['delivery_qty'])

df['daily_acc_score'] = (
    0.5 * df['z_deliv'] +
    0.3 * df['z_vol'] +
    0.2 * (1 - abs(df['return_1d']))
)

👉 More weight to delivery = smart money focus

🔁 STEP 3: MULTI-WINDOW ENGINE (KEY UPGRADE)

Define windows:

windows = [5, 10, 15, 20, 30]
🎯 For each window, compute:
for w in windows:
    df[f'acc_days_{w}'] = (df['daily_acc_score'] > 0.5).rolling(w).sum()
    df[f'tight_days_{w}'] = df['tight_range'].rolling(w).sum()
    df[f'vol_mean_{w}'] = df['vol_ratio'].rolling(w).mean()
✅ WINDOW-LEVEL ACCUMULATION SIGNAL
for w in windows:
    df[f'acc_signal_{w}'] = (
        (df[f'acc_days_{w}'] >= w * 0.6) &
        (df[f'tight_days_{w}'] >= w * 0.6) &
        (df[f'vol_mean_{w}'] > 1.1)
    )
🧱 STEP 4: ABSORPTION (MULTI-WINDOW)
df['buying_wick'] = (
    (df['close'] > df['open']) &
    (df['low'] < df['low'].shift(1))
)

for w in windows:
    df[f'absorption_{w}'] = (
        df['buying_wick'].rolling(w).sum() >= w * 0.4
    )
🧠 STEP 5: CONSISTENCY SCORE (VERY POWERFUL)

We now measure:

“How many windows show accumulation?”

acc_cols = [f'acc_signal_{w}' for w in windows]
abs_cols = [f'absorption_{w}' for w in windows]

df['acc_window_count'] = df[acc_cols].sum(axis=1)
df['abs_window_count'] = df[abs_cols].sum(axis=1)
🚀 FINAL SMART ACCUMULATION SIGNAL (5–30 DAY)
df['SMART_ACCUMULATION_MT'] = (
    (df['acc_window_count'] >= 2) &   # at least 2 windows agree
    (df['abs_window_count'] >= 2)
)
📊 STRENGTH SCORING (CRITICAL)
df['acc_strength_mt'] = (
    0.6 * (df['acc_window_count'] / len(windows)) +
    0.4 * (df['abs_window_count'] / len(windows))
)
🧠 INTERPRETATION (VERY IMPORTANT)
Strength	Meaning
> 0.7	🔥 Strong institutional accumulation
0.5–0.7	👀 Build-up phase
< 0.4	❌ Noise
🎯 DIFFERENT TYPES YOU’LL DETECT
🟢 Type 1: Fast Accumulation (5–10 days)
Quick smart money entry
Leads to fast breakout
🔵 Type 2: Base Building (15–30 days)
Long sideways
Stronger breakout later
🟣 Type 3: Re-Accumulation (after uptrend)
Continuation pattern
🔗 COMBINE WITH ENTRY (IMPORTANT)
df['FINAL_BUY'] = (
    df['SMART_ACCUMULATION_MT'].shift(1) &
    df['breakout_confirm']
)
⚠️ FILTERS (VERY IMPORTANT FOR NSE)
❌ Avoid operator-driven junk
df['liquid'] = df['volume'].rolling(20).mean() > 500000
❌ Avoid news spikes
df['no_news_spike'] = abs(df['return_1d']) < 0.08
✅ Final filter
df['VALID_SETUP'] = (
    df['SMART_ACCUMULATION_MT'] &
    df['liquid'] &
    df['no_news_spike']
)
🔥 WHY THIS IS POWERFUL

Compared to your earlier version:

Feature	Old	New
Single window	✅	❌
Multi-timescale	❌	✅
Persistence detection	❌	✅
Institutional behavior modeling	⚠️	✅
🧠 FINAL EDGE

You now detect:

📦 Multi-week accumulation
🧱 Supply absorption
⏳ Base formation before big moves
🎯 High probability breakout zones

👉 This is how real positional traders operate